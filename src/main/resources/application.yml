spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/users_reset
    username: postgres
    password: Admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    port: 587
    username: atalibcodinglevel@gmail.com
    password: glrabsbkjopwlviw
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false